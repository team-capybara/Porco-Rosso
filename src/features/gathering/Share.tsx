import { ShareProps } from './types/index';

const Share = (props: ShareProps) => {
  console.log(props);
  return (
    <div>
      <h1>Share Page</h1>
      {/* 모임 순간 공유 페이지 */}
    </div>
  );
};

export default Share;
