import { GalleryGridProps } from './types/index';

const GalleryGrid = (props: GalleryGridProps) => {
  console.log(props);
  return (
    <div>
      <h1>Gallery Grid Page</h1>
      {/* 순간 모음 그리드 나열 화면 */}
    </div>
  );
};

export default GalleryGrid;
